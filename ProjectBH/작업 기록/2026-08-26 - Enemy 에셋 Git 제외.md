# Enemy 에셋 Git 제외

- 작업 일자: 2026-08-26
- 요청 경로: `/Game/Enemy/Enemies`

## 변경 내용

Unreal Content Browser 경로를 프로젝트 파일 경로인 `/Content/Enemy/Enemies/`로 변환해 `.gitignore`에 추가했다.

이 폴더와 하위의 로컬 Enemy 에셋은 앞으로 새 Git 추적 대상에 포함되지 않는다.

## 주의

이미 Git에 커밋된 파일이 있다면 `.gitignore`만으로는 추적이 해제되지 않는다. 그 경우에는 Git 인덱스에서 별도로 제거해야 한다.
