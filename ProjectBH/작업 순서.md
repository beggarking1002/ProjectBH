- 체력·피해 기반
    - `UBHAttributeSet`에 `Health`, `MaxHealth` 추가
    - 체력 값 복제
    - `GameplayEffect` 기반 피해 적용
    - 적 더미 1개 생성
    - 호스트와 클라이언트가 같은 더미 체력 변화를 확인
- 기본 공격 입력과 애니메이션
    - `InputTag.Attack.Light` 추가
    - `IA_LightAttack`과 Mapping Context 연결
    - 공격 요청 시 공격 애니메이션 1타 재생
    - 공격 중복 실행 방지용 `State.Attacking` 태그 추가
- 서버 권한 타격 판정
    - 도끼에 공격 추적 시작·끝 기준점 추가
    - 공격 애니메이션의 타격 구간에 Anim Notify State 배치
    - 서버만 도끼 궤적 Trace 수행
    - 맞은 더미에 `GE_Damage` 적용
    - 클라이언트가 임의로 체력을 깎을 수 없도록 유지
- 2타 콤보
    - 1타 중 입력 버퍼를 받음
    - 입력이 있으면 2타 연결, 없으면 Idle 복귀
    - 공격 중 이동 제한·방향 제어 범위 결정
    - 호스트와 클라이언트 모두에게 공격 결과가 동일한지 검증
- 사망 처리
    - 체력 0에서 Dead 상태 태그 부여
    - 사망 애니메이션, 이동·입력 차단
    - 더미 Destroy 또는 간단한 리스폰
- 그 뒤에 무기 선택 구조
    - `DA_WeaponDefinition` 도입
    - 도끼와 검 두 종류 데이터 추가
    - `PlayerState`에 선택 무기 저장
    - 서버가 선택 결과에 맞는 무기를 장착